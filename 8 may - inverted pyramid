#include <iostream>
using namespace std;
int main() {
    int re;
    cin >> re ;
    for(int row = 0 ; row < re ; row++){
        for (int col = 1 ; col <= (re - row) ; col++){
            cout << "*";            
        }
        cout << endl;
    }

    return 0;
}
